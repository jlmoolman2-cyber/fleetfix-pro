import { createKnownFix, listKnownFixes } from "@/lib/iq200/knownFixService"; import { authenticateServerRequest, safeServerErrorResponse } from "@/lib/serverAuth";
export async function GET(request: Request) { try { return Response.json(await listKnownFixes(await authenticateServerRequest(request)), { headers: { "cache-control": "no-store" } }) } catch (error) { const response = safeServerErrorResponse(error); response.headers.set("cache-control", "no-store"); return response; } }
export async function POST(request: Request) { try { return Response.json(await createKnownFix(await authenticateServerRequest(request), await request.json()), { status: 201, headers: { "cache-control": "no-store" } }) } catch (error) { const response = safeServerErrorResponse(error); response.headers.set("cache-control", "no-store"); return response; } }
