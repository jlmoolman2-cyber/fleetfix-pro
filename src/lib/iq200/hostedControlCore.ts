import { createHash } from "node:crypto";
const SAFE_ID=/^[A-Za-z0-9_-]{1,128}$/;
export function validateHostedControlIds(...values:string[]){if(values.some(value=>!SAFE_ID.test(value)))throw new Error("INVALID_CONTROL_SCOPE")}
export function hostedIdempotencyKey(companyId:string,userId:string,jobId:string,sessionId:string,question:string){validateHostedControlIds(companyId,userId,jobId,sessionId);return createHash("sha256").update(`${companyId}\0${userId}\0${jobId}\0${sessionId}\0${question.trim().replace(/\s+/g," ")}`).digest("hex")}
export function nextHostedRateCount(stored:unknown,limit:number){if(!Number.isSafeInteger(limit)||limit<1||limit>10_000)throw new Error("INVALID_RATE_LIMIT");const count=stored===undefined?0:stored;if(!Number.isSafeInteger(count)||Number(count)<0||Number(count)>=Number.MAX_SAFE_INTEGER)throw new Error("INVALID_RATE_STATE");if(Number(count)>=limit)throw new Error("RATE_LIMITED");return Number(count)+1}
export function nextHostedCommissioningCount(stored:unknown){const count=stored===undefined?0:stored;if(!Number.isSafeInteger(count)||Number(count)<0)throw new Error("INVALID_COMMISSIONING_STATE");if(Number(count)>=1)throw new Error("COMMISSIONING_LIMIT");return 1}
